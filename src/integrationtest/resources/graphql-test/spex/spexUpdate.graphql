mutation spexUpdate ($id: ID!, $year: Year!, $title: String!) {
    spexUpdate(input: { id: $id, year: $year, title: $title }) {
        id
        year
        title
        revival
        posterUrl
        createdBy
        createdAt
        lastModifiedBy
        lastModifiedAt
    }
}
