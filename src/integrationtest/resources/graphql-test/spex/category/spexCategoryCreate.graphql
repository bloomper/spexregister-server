mutation spexCategoryCreate ($name: String!, $firstYear: Year!) {
    spexCategoryCreate(input: { name: $name, firstYear: $firstYear }) {
        id
        name
        firstYear
        createdBy
        createdAt
        lastModifiedBy
        lastModifiedAt
    }
}
