query spexCategoryEvents ($sourceId: ID!, $sinceInDays: Int = 90) {
    spexCategoryEvents (sourceId: $sourceId, sinceInDays: $sinceInDays) {
        id
        eventType
        sourceType
        createdBy
        createdAt
    }
}
