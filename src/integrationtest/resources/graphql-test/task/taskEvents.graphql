query taskEvents ($sourceId: ID!, $sinceInDays: Int = 90) {
    taskEvents (sourceId: $sourceId, sinceInDays: $sinceInDays) {
        id
        eventType
        sourceType
        createdBy
        createdAt
    }
}
