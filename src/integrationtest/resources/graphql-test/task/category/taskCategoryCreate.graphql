mutation taskCategoryCreate ($name: String!, $actorPresent: Boolean!) {
    taskCategoryCreate(input: { name: $name, actorPresent: $actorPresent }) {
        id
        name
        actorPresent
        createdBy
        createdAt
        lastModifiedBy
        lastModifiedAt
    }
}
