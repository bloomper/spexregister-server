mutation taskUpdate ($id: ID!, $name: String!) {
    taskUpdate(input: { id: $id, name: $name }) {
        id
        name
        createdBy
        createdAt
        lastModifiedBy
        lastModifiedAt
    }
}
