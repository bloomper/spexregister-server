mutation userUpdate ($id: ID!, $email: Email!) {
    userUpdate(input: { id: $id, email: $email }) {
        id
        email
        createdBy
        createdAt
        lastModifiedBy
        lastModifiedAt
    }
}
