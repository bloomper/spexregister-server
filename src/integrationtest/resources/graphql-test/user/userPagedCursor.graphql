query UserPagedCursor ($first: Int, $after: String, $last: Int, $before: String, $filter: String = "", $sort: [String] = ["id"], $direction: SortDirection = ASC) {
    userPaged (first: $first, after: $after, last: $last, before: $before, filter: $filter, sort: $sort, direction: $direction) {
        edges {
            cursor
            node {
                id
                email
            }
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
        totalCount
    }
}
