query SpexarePagedCursor ($first: Int, $after: String, $last: Int, $before: String, $filter: String = "", $sort: [String] = ["id"], $direction: SortDirection = ASC) {
    spexarePaged (first: $first, after: $after, last: $last, before: $before, filter: $filter, sort: $sort, direction: $direction) {
        edges {
            cursor
            node {
                id
                firstName
                lastName
            }
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
        totalCount
    }
}
