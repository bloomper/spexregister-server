query tagEvents ($sourceId: ID!, $sinceInDays: Int = 90) {
    tagEvents (sourceId: $sourceId, sinceInDays: $sinceInDays) {
        id
        eventType
        sourceType
        createdBy
        createdAt
    }
}
