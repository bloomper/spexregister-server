query tagPaged ($first: Int = 10, $filter: String = "") {
    tagPaged (first: $first, filter: $filter) {
        edges {
            cursor
            node {
                id
                name
                createdBy
                createdAt
                lastModifiedBy
                lastModifiedAt
            }
        }
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
    }
}
