mutation tagUpdate ($id: ID!, $name: String!) {
    tagUpdate(input: { id: $id, name: $name }) {
        id
        name
        createdBy
        createdAt
        lastModifiedBy
        lastModifiedAt
    }
}
