spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
  jpa:
    properties:
      hibernate:
        search:
          backend:
            directory:
              type: local-heap
