management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info
spring:
  jpa:
    open-in-view: false
  h2:
    console:
      enabled: false
  servlet:
    multipart:
      max-file-size: 10MB
